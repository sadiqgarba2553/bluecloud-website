## Changes
1. index.html — Full UI redesign (no emojis, no gradients, no glassmorphism — Adobe/Figma dark pro style). Fix AI (gemini-1.5-flash, correct request format, CORS fix in Electron).
2. main.js — Electron main process (webSecurity: false enables Gemini API, no title bar menu)
3. package.json — electron + electron-builder config
4. build.bat — one-click build script for exe
